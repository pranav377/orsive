import Channel from '@/ui/Chat/Channel';
import { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Pranava Mohan - Chat | Orsive',
};

export default function ChannelPage() {
    return <Channel />;
}
